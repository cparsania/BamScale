% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bam_read.R
\name{fragment_sizes}
\alias{fragment_sizes}
\title{Fragment-size distribution, aggregated in C++}
\usage{
fragment_sizes(
  file,
  param = NULL,
  threads = 1L,
  BPPARAM = BiocParallel::bpparam(),
  auto_threads = FALSE,
  max_fragment = 100000L,
  drop_mate_unmapped = TRUE
)
}
\arguments{
\item{file}{A BAM file path, \link[Rsamtools:BamFile]{Rsamtools::BamFile}, or a vector / \code{BamFileList}
of them.}

\item{param}{Optional \link[Rsamtools:ScanBamParam]{Rsamtools::ScanBamParam} (or a compatible list). Only
its \code{flag} and \code{mapqFilter} are honoured, applied per read in C++ -- e.g.
\code{Rsamtools::ScanBamParam(flag = Rsamtools::scanBamFlag( isSecondaryAlignment = FALSE, isUnmappedQuery = FALSE, isNotPassingQualityControls = FALSE))} to reproduce the standard ATAC
fragment-size filter.}

\item{threads}{Number of OpenMP threads for within-file decoding.}

\item{BPPARAM}{A \link[BiocParallel:BiocParallelParam]{BiocParallel::BiocParallelParam} for across-file parallelism
when \code{file} has more than one element.}

\item{auto_threads}{Logical; if \code{TRUE}, balance the thread / worker split.}

\item{max_fragment}{Integer; fragment sizes up to this value are tallied in a
dense histogram and larger ones in an overflow map. Every observed size is
returned exactly regardless of this value (default \code{1e5}).}

\item{drop_mate_unmapped}{Logical; if \code{TRUE} (default), reads whose mate is
unmapped (SAM flag \code{0x8}) are excluded -- matching \code{Rsamtools::scanBam},
which reports their \code{isize} as \code{NA} (no defined template length), so that
\code{table(abs(isize))} drops them. Set \code{FALSE} to tally \code{abs(TLEN)} over all
flag-passing reads.}
}
\value{
A \code{data.frame} with columns \code{fragment_size} (integer \code{abs(TLEN)}) and
\code{count} (numeric), one row per observed fragment size in ascending order.
For multiple input files, a named list of such data.frames.
}
\description{
Computes the paired-end fragment-size (insert-size) distribution -- a table of
\code{abs(TLEN)} counts -- by accumulating the histogram inside the multithreaded
C++ reader, without ever materialising the per-read insert sizes as an R
vector. This is the fast path for ATAC-seq / paired-end fragment-size QC on
large BAMs: the histogram is folded together inside the OpenMP threads and only
the compact \verb{(fragment_size, count)} table crosses back into R (cf.
\code{scanBam(what = "isize")} followed by \code{table(abs(isize))}, which materialises
every insert size in R first).
}
\seealso{
\code{\link[=bam_read]{bam_read()}} for reading records, \code{\link[=bam_count]{bam_count()}} for per-chromosome
counts.
}
