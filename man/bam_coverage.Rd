% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bam_read.R
\name{bam_coverage}
\alias{bam_coverage}
\title{Per-base coverage, aggregated in C++}
\usage{
bam_coverage(
  file,
  param = NULL,
  threads = 1L,
  BPPARAM = BiocParallel::bpparam(),
  auto_threads = FALSE
)
}
\arguments{
\item{file}{A BAM file path, \link[Rsamtools:BamFile]{Rsamtools::BamFile}, or a vector / \code{BamFileList}
of them.}

\item{param}{Optional \link[Rsamtools:ScanBamParam]{Rsamtools::ScanBamParam} (or a compatible list). Only its
\code{flag} and \code{mapqFilter} are honoured, applied per read in C++. With the default
\code{NULL} the filter matches \code{readGAlignments()} (drop only unmapped reads). Note
that \code{which} (region) restriction is not applied.}

\item{threads}{Number of OpenMP threads for within-file decoding.}

\item{BPPARAM}{A \link[BiocParallel:BiocParallelParam]{BiocParallel::BiocParallelParam} for across-file parallelism
when \code{file} has more than one element.}

\item{auto_threads}{Logical; if \code{TRUE}, balance the thread / worker split.}
}
\value{
An \link[IRanges:RleList]{IRanges::RleList} (\code{SimpleRleList}) of integer coverage, one element
per BAM-header sequence in header order, each of length equal to the header
sequence length (contigs with no reads are all-zero runs). For multiple input
files, a named list of such \code{RleList}s.
}
\description{
Computes per-base read coverage by folding a per-contig difference array inside
the multithreaded C++ reader and run-length encoding the result, without ever
materialising the alignments as an R \code{GAlignments} object. This is the fast path
for coverage / bigWig generation on large BAMs: the standard route
(\code{GenomicAlignments::coverage(readGAlignments(file))}) builds a full
\code{GAlignments} in R first, which is the dominant cost on 100M+-read files.
}
\details{
The result is \strong{byte-identical} (\code{identical()}) to
\code{GenomicAlignments::coverage(GenomicAlignments::readGAlignments(file))}: CIGAR
operations \code{M}, \code{=}, \code{X} and \code{D} contribute to coverage, \code{N} introduces a gap,
and \code{I}/\code{S}/\code{H}/\code{P} are ignored; only unmapped reads (SAM flag \code{0x4}) are
dropped by default -- secondary, supplementary, duplicate and QC-fail reads are
counted, exactly as \code{readGAlignments()} does.
}
\seealso{
\code{\link[=fragment_sizes]{fragment_sizes()}}, \code{\link[=mapq_dist]{mapq_dist()}}, \code{\link[=bam_read]{bam_read()}}.
}
