% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bam_read.R
\name{bam_coverage_bigwig}
\alias{bam_coverage_bigwig}
\title{Write per-base coverage to a bigWig, computed in C++}
\usage{
bam_coverage_bigwig(
  file,
  outfile,
  param = NULL,
  threads = 1L,
  BPPARAM = BiocParallel::bpparam(),
  auto_threads = FALSE,
  n_zooms = 10L,
  compress_level = -1L,
  parallel = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{file}{A BAM file path, \link[Rsamtools:BamFile]{Rsamtools::BamFile}, or a vector / \code{BamFileList}
of them.}

\item{outfile}{Output bigWig path(s); must have the same length as \code{file}.}

\item{param}{Optional \link[Rsamtools:ScanBamParam]{Rsamtools::ScanBamParam} (or a compatible list). Only its
\code{flag} and \code{mapqFilter} are honoured, applied per read in C++. With the default
\code{NULL} the filter matches \code{readGAlignments()} (drop only unmapped reads).}

\item{threads}{Number of OpenMP threads for within-file decoding.}

\item{BPPARAM}{A \link[BiocParallel:BiocParallelParam]{BiocParallel::BiocParallelParam} for across-file parallelism
when \code{file} has more than one element.}

\item{auto_threads}{Logical; if \code{TRUE}, balance the thread / worker split.}

\item{n_zooms}{Maximum number of bigWig zoom levels to generate (default \code{10}).}

\item{compress_level}{Integer zlib deflate level for the bigWig blocks: \code{-1}
(default) uses zlib's default (level 6, matching other bigWig writers); \code{1}
to \code{9} trade file size for speed (\code{1} is the fastest write and gives the
largest file). Coverage values are unaffected -- only file size and write
time change.}

\item{parallel}{Logical; if \code{TRUE} (default), compress the bigWig data and zoom
blocks across the OpenMP \code{threads} (deferred parallel compression). The output
is byte-identical to serial compression. Set \code{FALSE} for the classic
single-threaded write.}

\item{verbose}{Logical; if \code{TRUE}, print a per-phase timing breakdown
(coverage compute / interval write / zoom + index finalize) to stderr.}
}
\value{
The output path(s), invisibly.
}
\description{
Computes per-base coverage (identical to \code{\link[=bam_coverage]{bam_coverage()}}) and writes it
directly to a bigWig file entirely in C++ via the bundled libBigWig, without
ever materialising the coverage as an R object. This is the fast path for
generating coverage tracks from large BAMs: the usual route
(\code{rtracklayer::export.bw(bam_coverage(file), out)}) round-trips a large
\code{RleList} through R, and the serialisation dominates. Only non-zero coverage
runs are written (uncovered bases are implicitly zero, the bigWig convention);
importing the file reconstructs full-length coverage from the header lengths.
}
\details{
The written values are identical to
\code{GenomicAlignments::coverage(GenomicAlignments::readGAlignments(file))}
(\code{M}/\code{=}/\code{X}/\code{D} contribute, \code{N} is a gap, \code{I}/\code{S}/\code{H}/\code{P} are ignored; only
unmapped reads are dropped by default). Coverage values are integers stored as
the bigWig \code{float} type, which is exact for depths up to \code{2^24}.
}
\seealso{
\code{\link[=bam_coverage]{bam_coverage()}} for the in-memory \code{RleList}.
}
