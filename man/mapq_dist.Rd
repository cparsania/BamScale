% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bam_read.R
\name{mapq_dist}
\alias{mapq_dist}
\title{Mapping-quality (MAPQ) distribution, aggregated in C++}
\usage{
mapq_dist(
  file,
  param = NULL,
  threads = 1L,
  BPPARAM = BiocParallel::bpparam(),
  auto_threads = FALSE
)
}
\arguments{
\item{file}{A BAM file path, \link[Rsamtools:BamFile]{Rsamtools::BamFile}, or a vector / \code{BamFileList}
of them.}

\item{param}{Optional \link[Rsamtools:ScanBamParam]{Rsamtools::ScanBamParam} (or a compatible list). Only
its \code{flag} and \code{mapqFilter} are honoured, applied per read in C++. Note that
a non-zero \code{mapqFilter} removes reads below that MAPQ from the distribution.}

\item{threads}{Number of OpenMP threads for within-file decoding.}

\item{BPPARAM}{A \link[BiocParallel:BiocParallelParam]{BiocParallel::BiocParallelParam} for across-file parallelism
when \code{file} has more than one element.}

\item{auto_threads}{Logical; if \code{TRUE}, balance the thread / worker split.}
}
\value{
A \code{data.frame} with columns \code{mapq} (integer, 0-255) and \code{count}
(numeric), one row per observed MAPQ value in ascending order. For multiple
input files, a named list of such data.frames.
}
\description{
Computes the MAPQ distribution -- a table of per-value alignment-count -- by
accumulating the histogram inside the multithreaded C++ reader, without ever
materialising the per-read MAPQ values as an R vector. This is the fast path
for mapping-quality QC on large BAMs: each thread tallies MAPQ into a private
256-slot histogram inside the OpenMP region and only the compact
\verb{(mapq, count)} table crosses back into R (cf. \code{scanBam(what = "mapq")}
followed by \code{table(mapq)}, which materialises every MAPQ in R first).
}
\seealso{
\code{\link[=fragment_sizes]{fragment_sizes()}} for the fragment-size distribution, \code{\link[=bam_count]{bam_count()}}
for per-chromosome counts.
}
